# Cloudflare Pages redirects — https://developers.cloudflare.com/pages/configuration/redirects/
# The sample audit moved from the Work section into Services (2026-06).
/work/sample-audit/  /services/sample-audit/  301

# Older service names now resolve to the dedicated service pages.
/services/implementation-sprint/  /services/ai-implementation/  301
/services/managed-services/     /services/managed-ai-services/  301

# The AI-Ready Score app (Vercel) is reverse-proxied onto the apex — /score/*
# and the PRINTED book-QR short links /s/* — by functions/score/[[path]].js and
# functions/s/[[path]].js (origin = SCORE_ORIGIN env var). Served same-origin,
# no redirect. Do NOT add a /score or /s redirect here; it would pre-empt the
# Functions. The /s/* QR contract is permanent — never drop those routes.

# Report links emailed before the app's basePath /score (2026-07) pointed at
# apex /report/<token>, which the apex never served. Persistent report URLs
# now live at /score/report/<token>; rescue the old form.
/report/*  /score/report/:splat  301

# The retired essay is replaced by the related guide to human review.
/blog/the-person-still-signs  /blog/human-in-the-loop-ai-systems/  301
/blog/the-person-still-signs/  /blog/human-in-the-loop-ai-systems/  301
