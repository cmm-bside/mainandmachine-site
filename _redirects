# Cloudflare Pages redirects — https://developers.cloudflare.com/pages/configuration/redirects/
# The sample audit moved from the Work section into Services (2026-06).
/work/sample-audit/  /services/sample-audit/  301

# Three service detail pages consolidated into /services/ anchored sections (2026-06).
/services/ai-readiness-audit/   /services/#audit    301
/services/implementation-sprint/  /services/#sprint  301
/services/managed-services/     /services/#managed  301

# The AI-Ready Score app lives on Vercel at score.mainandmachine.com; these
# keep the SPEC'd /score path and the PRINTED book-QR short links (/s/ch05
# etc.) working on the main domain forever. The /s/* rule is the permanent
# contract behind the QR codes in The AI-Ready Owner — never remove it;
# where it points is the only thing that may change.
/score   https://score.mainandmachine.com/score    302
/score/  https://score.mainandmachine.com/score    302
/s/*     https://score.mainandmachine.com/s/:splat 301
